{
  "name": "seamuns",
  "private": true,
  "description": "SEAMUNs — MUN conference tracker (static site)",
  "scripts": {
    "build": "node scripts/generate-env.js && node scripts/prepare-vercel-output.js"
  }
}
